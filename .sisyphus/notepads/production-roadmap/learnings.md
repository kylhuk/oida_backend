- 2026-04-25: `meta.schema_change_registry` can be evolved safely by adding a follow-on ClickHouse migration and making `internal/migrate.HTTPRunner.RecordSchemaChange()` detect the live column set before inserting. That keeps fresh replays working across `0028_schema_change_registry.sql` and later registry extensions without rewriting applied SQL.
- 2026-04-25: Bootstrap schema safety now treats `meta.schema_change_registry` like the migration ledger itself: install/verify both check a frozen column contract, and migration headers can persist queryable `diff_summary`, `compatibility_notes`, and `approval_ref` metadata for rollout planning.
- 2026-04-25: Task 2 can stay minimal by storing pipeline definitions in `meta.pipeline_registry` and persisting deterministic lifecycle snapshots in `ops.pipeline_run`, while reusing `executePromoteInputsWithRunner()` so orchestration stays in `cmd/control-plane` and `internal/promote` remains a pure planner.
- 2026-04-25: Task 3 fit cleanly into the existing `cmd/control-plane/jobs_promote.go` incremental-selection path by freezing each promote run into per-source, per-bronze-table half-open windows and persisting those windows in a `ReplacingMergeTree` checkpoint ledger (`ops.promote_checkpoint`). That keeps historical replay queryable and makes reruns skip already-succeeded windows instead of replaying the whole batch.
- 2026-04-25: Task 4 stayed smallest by reusing existing ledgers instead of introducing a brand-new queue service: `ops.crawl_frontier` now honors bounded retry backoff via `next_fetch_at`, and `ops.parse_checkpoint` now records `retry` versus `dead_letter` state with attempt counts and failure details so poison raws stay queryable without starving healthy work.
- 2026-04-25: Task 5 stayed smallest by treating `ops.job_run`, `ops.fetch_log`, and `ops.parse_log` as the observability spine: a shared `internal/observability` helper now standardizes `X-Request-ID`/`correlation_id`, workers persist that id in fetch metadata plus operational ledgers, and the API/renderer expose a dashboard-safe live tail by unioning those persisted rows instead of scraping raw process logs.
