- 2026-04-25: The host shell in this environment does not expose a local `go` binary and the Go LSP workspace was not active for changed files, so formatting and verification had to run through `golang:1.23` Docker containers plus `docker compose` for the bootstrap lifecycle.
- 2026-04-25: The local `golang:1.23` image in this environment required invoking `/usr/local/go/bin/go` and `/usr/local/go/bin/gofmt` via an explicit `/bin/sh` entrypoint, and `go build ./...` needed `-buildvcs=false` because containerized VCS stamping could not inspect the host checkout metadata.
- 2026-04-25: Extending parse retry handling changed the worker-test contract from count-based checkpoint lookups to `FINAL` checkpoint snapshots, so the package tests had to model the newer ledger shape instead of the old success-only checkpoint query.
- 2026-04-25: Adding `/v1/internal/worker-tail` changed the authoritative API contract outputs, so `testdata/fixtures/contracts/api_v1_schema.json`, `docs/api-reference.md`, and the README route inventory had to be regenerated from the route metadata to keep contract parity tests green.
