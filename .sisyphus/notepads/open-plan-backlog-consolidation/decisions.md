- 2026-04-21 task 5: kept bootstrap test names and validation flow intact, but replaced the bootstrap-local archetype compatibility logic with calls into `internal/parser`; this preserves existing catalog/runtime behavior while making the parser compatibility matrix the single source of truth for task-5 scope.
- 2026-04-21 task 6: did not add a new scheduler or migration. The existing `runAutomaticSyncTick()` loop remains the planner boundary, while canonical-url dedupe is enforced in application logic by normalizing entrypoints in `internal/discovery` and using control-plane update-or-insert frontier seeding keyed by `(source_id, canonical_url)`.
- 2026-04-21 task 7: kept the processed-document ledger change inside `cmd/worker-parse` runtime/query logic and its package tests instead of editing ClickHouse migrations because `migrations/clickhouse/0018_parse_checkpoint_ledger.sql` already provides the checkpoint table contract needed for incremental parse.
- 2026-04-21 task 7: used checkpoint-aware raw-document selection plus the existing post-load `hasSuccessfulParseCheckpoint()` guard. The query-level filter removes repeat work on second runs, while the in-loop check still protects bronze idempotency if concurrent workers race on the same raw document.
- 2026-04-21 task 8: kept the incremental promote change inside `cmd/control-plane/jobs_promote.go` and its tests instead of adding a migration or new workflow boundary; the existing bronze-to-promote flow now scopes work by source and change window before `internal/promote` planning starts.
- 2026-04-21 task 8: left `internal/promote` canonical row generation untouched so replay stability continues to come from the existing SHA-256 business-identity IDs while control-plane selection narrows which bronze slices are sent into that planner.
- 2026-04-21 task 10: kept family-template expansion inside the existing compiled-catalog -> discovery-candidate path rather than adding a new governance table or runnable-seed shortcut. Family runs now emit deterministic child-source definitions into `meta.discovery_candidate.attrs`, and approval gating remains the only path toward `meta.source_registry` materialization.
- 2026-04-21 task 10: chose backward-compatible compiled-catalog hydration over rewriting `seed/source_catalog_compiled.json` in this task. The loader backfills the new family transport/scope/child-source metadata from `catalog.entries`, so control-plane jobs and bootstrap tests see one stable contract whether the compiled artifact is old or freshly regenerated.
- 2026-04-21 silver rollout task 8: kept the fix inside `cmd/control-plane/jobs_http_sources.go` plus bootstrap RBAC instead of adding a migration or new scheduler; existing frontier rows are refreshed with non-key mutation only, and due-time gating now reuses `last_attempt_at` from the leased frontier contract to avoid forbidden ClickHouse key updates.
- 2026-04-21 silver rollout task 8 review: approved after verifying the user-visible paths directly (`test/e2e/pipeline_test.go` plus targeted E2E and control-plane unit runs), not just internal helpers.
- 2026-04-25 F1 audit: APPROVE — task 13 stays within the HTTP fixture/docs/E2E boundary. `docker-compose.yml` wires `http-fixture` to static files under `testdata/http-fixture`, `manifest.json` enumerates the seven payloads with the ACLED credential gate, and `test/e2e/pipeline_test.go` asserts both the manifest and per-source payload shape. No TODO/FIXME markers were found in the fixture subtree.
- 2026-04-25 F2 review: APPROVE — the HTTP fixture slice remains aligned with `test/e2e/pipeline_test.go`; the manifest and payload JSON files are minimal, valid, and internally consistent, and the focused manifest-vs-payload consistency check passed.
- 2026-04-25 F4 scope review: APPROVE — task 13 stays within the compose HTTP fixture/E2E/docs boundary. The concrete task-13 artifacts are limited to `docker-compose.yml`, `test/e2e/pipeline_test.go`, `README.md`, `docs/capability-matrix.md`, `docs/capability-matrix.json`, and `testdata/http-fixture/*`; `manifest.json` lists only the seven expected HTTP sources with ACLED credential-gated, the docs explicitly say maritime/aviation/space remain on non-HTTP fixture packs, and `git diff --name-only -- internal/packs/maritime internal/packs/aviation internal/packs/space cmd/control-plane/jobs_{maritime,aviation,space}.go` returned no runtime-pack changes.
- 2026-04-25 F4 scope review: APPROVE — completion-plan task 7 stayed inside the place-acquisition boundary. The implementation surface is confined to `cmd/control-plane/jobs_place_sync.go` plus help/coverage tests (`cmd/control-plane/jobs_place_sync_test.go`, `cmd/control-plane/main_test.go`, and the existing `test/e2e/pipeline_test.go` run-once help assertion); the runtime code only downloads geoBoundaries/GeoNames assets, stages them under `place-datasets/<job>/current`, and records provenance/checksum metadata in `ops.job_run` without changing `jobs_place_build.go` or other place materialization logic. Focused proof passed via `docker run --rm -v /home/hal9000/docker/oida_backend:/app -w /app golang:1.23 /usr/local/go/bin/go test ./cmd/control-plane -run 'TestRunGeoBoundariesSyncStagesArtifactAndRecordsJobRun|TestRunGeoNamesSyncStagesArtifactsAndSupportsReruns|TestRunOnceUsageListsSupportedJobs|TestRunOnceUsageListsPlaceSyncJobs' -count=1`.
- 2026-04-25 QA: the Task 2 verification decision is to treat `go build -buildvcs=false ./...` as the authoritative build check in this containerized environment, since the code itself passed the requested test slices and the only failing build mode was VCS stamping from the mounted checkout.
